Simple pattern for looping through a count to create a template in TF.

```
terraform init
terraform plan
terraform apply
```

One template with a count is rendered by another. That rendering is itself rendered into a local file.
